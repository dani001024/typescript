// @ts-check
// 자바스크립트 파일에 JSDoc 코멘트를 더하면 타입스크립트 처럼 사용 가능
/**
 * Initializes the project
 * @param {object} config
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns boolean
 */
export function init(config) {
    return true;
}
/**
 * Exits the program
 * @param {number} code
 * @returns number
 */
export function exit(code) {
    return code + 1;
}
